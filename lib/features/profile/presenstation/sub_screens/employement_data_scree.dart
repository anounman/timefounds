import 'package:flutter/material.dart';

class EmployementDataScree extends StatelessWidget {
  const EmployementDataScree({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Employement Data')),
      body: const Center(child: Text('Employement Data Screen')),
    );
  }
}
