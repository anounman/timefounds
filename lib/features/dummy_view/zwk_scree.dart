import 'package:flutter/material.dart';

class ZWkScreen extends StatelessWidget {
  const ZWkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ZWk')),
      body: const Center(child: Text('ZWk Screen')),
    );
  }
}
